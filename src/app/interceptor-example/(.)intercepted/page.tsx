const InterceptorPage = () => {
  return (
    <h3>
      Page intercepted by using (.) as prefix of the folder you want to
      intercept. The interception folder should in inside the source one.
    </h3>
  );
};

export default InterceptorPage;
